
{
  "name": "etheria-portal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate-feeds && vite",
    "generate-feeds": "node scripts/generate-feeds.js",
    "build": "npm run generate-feeds && tsc && vite build && cd qqbx && npm install && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^0.400.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "@vitejs/plugin-react": "^4.0.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
